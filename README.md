# vbt-data

Public shell for a personal market-tracking dashboard.

A scheduled GitHub Action fetches market data for a set of tickers
(`data/`) and publishes a static dashboard via GitHub Pages. Pipeline
logic runs from a separate private repository.
